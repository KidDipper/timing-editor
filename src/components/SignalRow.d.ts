import type { Signal } from "../types";
type Props = {
    index: number;
    sig: Signal;
};
export default function SignalRow({ index, sig }: Props): import("react/jsx-runtime").JSX.Element;
export {};
